let count = 0;

count = count + 1;

// Line 1 is a variable declaration, creating the count variable with an initial value of 0
// Describe what line 3 is doing, in particular focus on what = is doing
// Line 3 is reassigning the value for variable count by adding 1 to variable count, so the variable count will evaluate to 1.
// The single equal sign (=) is an assignment operator. It assigns the value on the right side to the variable on the left side.